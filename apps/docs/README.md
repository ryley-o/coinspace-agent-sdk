# CoinSpace Agent SDK docs

The docs site for [coinspace-agent-sdk](../../README.md) — Next.js + [Nextra](https://nextra.site).

```bash
pnpm install
pnpm --filter @coinspace/docs run dev
```

Content lives in `app/**/page.mdx`; nav ordering/titles in `app/**/_meta.ts`.
