{
  "name": "coinspace-agent-sdk",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "pnpm -r --filter=./packages/* run build",
    "lint": "pnpm -r run lint",
    "dev:docs": "pnpm --filter @coinspace/docs run dev"
  },
  "packageManager": "pnpm@11.25.0",
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
